# SWIFT BayesFlow project — install: pip install -r requirements.txt

bayesflow==2.0.11      # amortized inference (v2 API)
keras>=3.5             # multi-backend Keras 3
torch>=2.0             # Keras backend (set KERAS_BACKEND=torch)

numpy>=2.0
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
